#include <stdio.h>
#include <ctype.h>

int countVowels(char str[]) {
	int i;
	int count;
	int ch;

	while (str[i] != '\0') {
		ch = toupper (str[i]);
		if ((c == 'A') || (c == 'E') || (c == 'I') || (c == 'O') || (c == 'U'))
      		vowels++;
		i++;
  }
  return vowels;
}


R2, L3  ==> 5
R2, R2, R2 => 2
R5, L5, R5, R3 ==> 12




	

// true = prev



